# v0.9.4 升级说明

## 本次升级

- 新增“模型定价”页面，只列出本机实际使用过的模型，并按累计 Token 从高到低排列。每个模型均可独立设置币种、新输入、缓存读入、缓存写入和输出单价。
- 取消应用内置 API 价格和自动回退逻辑。未填写的价格保持“未定价”，后续出现新模型时无需升级应用即可手动补充价格。
- 新增高峰期定价：可设置本机开始时间、结束时间和收费倍率，并通过白名单明确指定适用模型。未加入白名单的模型始终按常规定价，支持跨午夜时段。
- “本机活动”新增 Codex Token 专用视图，可按今日、7 天、30 天和累计查看新输入、缓存输入、缓存写入、可见输出、推理输出及模型排行。
- 继续使用严格的账号读取 RPC 白名单。刷新只执行初始化及账号读取，不创建对话、发送聊天消息或发起模型推理。

## 安装与升级

- Windows x64 安装包：`AI-Token-Usage-Monitor-0.9.4-Setup.exe`
- 可直接运行安装包从 v0.9.3 升级；应用标识和本机数据目录保持不变。
- 安装包未进行商业代码签名，Windows 可能显示未知发布者提示。
- 按本次发布要求不提供 SHA-256 校验文件。

## 验证范围

已完成 TypeScript 检查、97 项前端测试、Rust 测试、格式检查、Clippy 和生产构建。干净 Windows 首装、v0.9.3 → v0.9.4 升级、卸载后数据保留及缺少 WebView2 的场景仍需单独进行人工验收。
