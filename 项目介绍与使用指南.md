# AI Token 用量监控：项目介绍与使用指南

简体中文 | [English](README.en.md)

## 项目作用

AI Token 用量监控是一款面向 Windows 的本地优先桌面工具，用来同时查看 Codex 账号级额度、账号每日 Token 活动，以及当前电脑可读取的 Codex、Claude Code、OpenCode、WorkBuddy 和 WorkBuddy AI 本地活动。账号数据与本机数据始终分开展示，避免把本地记录误认为整个账号的完整账单或额度。

当前发布版本为 **v0.9.2**。

## 安装

1. 在 GitHub 仓库右侧打开 **Releases**。
2. 下载 `AI Token 用量监控-0.9.2-Setup.exe`。
3. 运行安装程序并按提示完成安装。

安装包尚未进行商业代码签名，Windows SmartScreen 可能显示“未知发布者”。请先核对 Release 页面给出的 SHA-256，再决定是否运行。

## 基本使用

1. 启动应用，等待首次本机索引完成。历史记录较多时可能需要几十秒。
2. 在“总览”查看 Codex 账号级额度和账号近期每日活动。
3. 在“本机活动”查看按 AI 来源、模型、项目和时间归因的当前电脑数据。
4. 使用今日、7 天、30 天、累计或自定义日期筛选，并可在单选/多选模式间切换来源。
5. 在“费用”查看基于离线公开价格快照的估算；未定价或信息不足的记录不会被伪装成零费用。
6. 在“设置”调整刷新周期、主题、通知、托盘行为和本机项目合并规则。
7. 关闭主窗口后应用默认留在系统托盘；需要彻底结束时使用托盘“退出”。

## 数据与隐私

- 不读取 Codex 的 `auth.json`，不保存访问令牌。
- 不持久化提示词、回答正文或工具输出。
- 本机来源索引只保存用量派生信息，不代表其他电脑或云端任务的完整活动。
- 设置和派生索引位于当前 Windows 用户的应用数据目录；维护操作不会改写原始 AI 会话日志。

## 从源码运行

需要 Node.js 22 或更高版本、Rust 工具链及 Windows 构建环境。

```powershell
npm install
npm test
npm run typecheck
npm run desktop:dev
```

构建 Windows 安装包：

```powershell
npm run desktop:build
```

## 发布状态

本仓库保存 v0.9.2 最终源码，安装包通过 GitHub Release 分发。本机构建和自动化检查不等同于干净 Windows 机器上的首次安装、升级、卸载、数据保留和 WebView2 路径验收。
